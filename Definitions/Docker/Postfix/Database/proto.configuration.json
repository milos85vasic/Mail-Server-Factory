{
  "TYPE": "Postgres",
  "HOST": "{{SERVER.POSTFIX.DBHOST}}",
  "PORT": "{{DB.DB_PORT}}",
  "USER": "{{DB.DB_USER}}",
  "PASSWORD": "{{DB.DB_PASSWORD}}",
  "NAME": "{{SERVER.POSTFIX.DBNAME}}",
  "SQL": [
    "init.sql"
  ]
}