{
  "TYPE": "Postgres",
  "HOST": "{{SERVER.DOVECOT.DBHOST}}",
  "PORT": "{{DB.DB_PORT}}",
  "USER": "{{DB.DB_USER}}",
  "PASSWORD": "{{DB.DB_PASSWORD}}",
  "NAME": "{{SERVER.DOVECOT.DBNAME}}",
  "SQL": [
    "init.sql"
  ]
}