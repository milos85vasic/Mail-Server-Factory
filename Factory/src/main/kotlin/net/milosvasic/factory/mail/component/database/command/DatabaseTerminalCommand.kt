package net.milosvasic.factory.mail.component.database.command

import net.milosvasic.factory.mail.terminal.TerminalCommand

abstract class DatabaseTerminalCommand(command: String) : TerminalCommand(command)